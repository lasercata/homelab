services:
  gitlab:
    image: gitlab/gitlab-ce:latest
    container_name: gitlab
    restart: unless-stopped
    hostname: gitlab.${DOMAIN}
    environment:
      GITLAB_OMNIBUS_CONFIG: |
        # Add any other gitlab.rb configuration here, each on its own line
        external_url http://gitlab.${DOMAIN}

    ports:
      # - '80:80'  # Already configured using nginx-proxy-manager in the network `docker-network`.
      # - '443:443'
      - '22:22'

    networks:
      - docker-network

    volumes:
      - '/srv/docker/volumes/nginx-proxy-manager/config:/etc/gitlab'
      - '/srv/docker/volumes/nginx-proxy-manager/logs:/var/log/gitlab'
      - '/srv/docker/volumes/nginx-proxy-manager/data:/var/opt/gitlab'

    shm_size: '256m'

networks:
  docker-network:
    external: true
